import { Text, View } from "react-native";

export const EmergencyActionPlan = () => {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
          <Text>Emergency Action Plan</Text>
        </View>
      );
};

export default EmergencyActionPlan;